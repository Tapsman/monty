#include "monty.h"
/**
 * f_stack - A function that will print at the top
 * @head: Is the head of the stack
 * @counter: line_number
 * Return: Nothing
 */
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
