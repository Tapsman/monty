#include "monty.h"
/**
 * f_nop - A function that does none
 * @head: Is the head stack
 * @counter: Is the line number
 * Return: Nothing
 */
void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
